dependencies {
}

gradle.taskGraph.whenReady {
    tasks.each { task ->
        task.enabled = true
        if ("bootJar" == task.name) {
            task.enabled = false
        }else if ("bootRun" == task.name) {
            task.enabled = false
        }
    }
}

